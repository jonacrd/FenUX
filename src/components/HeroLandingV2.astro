---
const { title, subtitle, badges=[], image="/images/landing-hero.webp", ctas=[] } = Astro.props;
---
<section class="section noise" style="position:relative">
  <div class="mesh"></div>
  <div class="grid" style="row-gap:24px">
    <header class="reveal">
      <h1 class="kick kinetic split">{title}</h1>
      <p style="opacity:.9">{subtitle}</p>
      <div class="badges">{badges.map(b=><span class="badge">{b}</span>)}</div>
      <div style="display:flex;gap:10px;flex-wrap:wrap;margin-top:14px">
        {ctas.map(c => <a class={`btn mag ripple ${c.alt?'outline':''}`} href={c.href} {...c.attrs}>{c.label}</a>)}
      </div>
    </header>
    <div class="parallax" data-parallax style="min-height:280px">
      <img class="hero-media" data-d="1" src={image} alt="" />
      <img class="layer card" data-d="0.6" src="/images/landing-shot-1.webp" style="left:6%;top:6%;width:44%" alt="" />
      <img class="layer card" data-d="0.9" src="/images/landing-shot-2.webp" style="right:6%;bottom:6%;width:46%" alt="" />
    </div>
  </div>
</section>
