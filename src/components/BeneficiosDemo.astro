---
// BeneficiosDemo.astro - Sección de beneficios reutilizable para demos
export interface Props {
  title?: string;
  subtitle?: string;
  benefits?: Array<{
    icon: string;
    title: string;
    description: string;
  }>;
}

const { 
  title = "¿Por qué elegir nuestro sistema?",
  subtitle = "Beneficios que transformarán tu negocio",
  benefits = [
    {
      icon: "🚀",
      title: "Implementación rápida",
      description: "Sistema operativo en menos de 48 horas, sin complicaciones técnicas."
    },
    {
      icon: "💰",
      title: "ROI inmediato",
      description: "Comienza a generar leads desde el primer día de implementación."
    },
    {
      icon: "📱",
      title: "Fácil de usar",
      description: "Interfaz intuitiva que cualquier persona puede manejar sin capacitación."
    },
    {
      icon: "🔧",
      title: "Soporte completo",
      description: "Acompañamiento personalizado durante los primeros 7 días."
    }
  ]
} = Astro.props;
---

<section class="py-20 bg-gradient-to-b from-brand-mid/20 to-brand-dark/30" id="beneficios">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="font-bold text-4xl md:text-5xl text-white mb-6">
        {title}
      </h2>
      <p class="text-xl text-neutral-200/90 max-w-3xl mx-auto">
        {subtitle}
      </p>
    </div>

    <!-- Grid de beneficios -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {benefits.map((benefit, index) => (
        <div 
          class="group text-center p-6 rounded-2xl bg-white/5 backdrop-blur-sm border border-white/10 hover:bg-white/10 transition-all duration-300 hover:scale-105"
          style={`animation-delay: ${index * 0.1}s`}
        >
          <!-- Icono -->
          <div class="text-6xl mb-4 group-hover:scale-110 transition-transform duration-300">
            {benefit.icon}
          </div>
          
          <!-- Título -->
          <h3 class="text-xl font-semibold text-white mb-3 group-hover:text-[#FF8A00] transition-colors duration-300">
            {benefit.title}
          </h3>
          
          <!-- Descripción -->
          <p class="text-neutral-200/80 leading-relaxed">
            {benefit.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  /* Animación de entrada */
  .group {
    opacity: 0;
    transform: translateY(30px);
    animation: slideInUp 0.6s ease-out forwards;
  }
  
  @keyframes slideInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
