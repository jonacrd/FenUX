---
// Home WOW - Fenux
import Base from '../layouts/Base.astro';
import LeadForm from '../components/LeadForm.astro';

// Configuración de la página
const pageConfig = {
  title: "Fenux — Soluciones Web y Apps que Impulsan tu Negocio",
  description: "Diseño premium, automatización y ventas en un solo lugar. Demo sin pago adelantado.",
  canonical: "https://fenux.com",
  ogImage: "/og/home.png"
};

// Servicios principales
const services = [
      {
        title: "Rediseño Web",
    description: "Modernizamos y mejoramos conversión de tu sitio actual",
    icon: "📊",
        href: "/es/servicios/redisenio"
      },
      {
        title: "Landing Pages",
    description: "Alta conversión en 5 días, sin pago adelantado",
        icon: "🎯",
        href: "/es/servicios/landing-portfolios"
      },
      {
        title: "Apps Web",
    description: "Módulos completos: inventarios, pedidos, reportes",
        icon: "💻",
        href: "/es/servicios/apps-web"
      }
];

// Qué incluye
const includes = [
  {
    icon: "📊",
    title: "Panel y módulos a medida",
    description: "Usuarios, pedidos, inventario y más. Todo personalizado para tu negocio."
  },
  {
    icon: "⚡",
    title: "Automatizaciones",
    description: "Emails, hojas de cálculo y WhatsApp. Todo se ejecuta automáticamente."
  },
  {
    icon: "🔗",
    title: "Integraciones",
    description: "Sheets, pagos, APIs. Conectamos con todas tus herramientas actuales."
  },
  {
    icon: "🛠️",
    title: "Soporte de arranque",
    description: "7 días de acompañamiento para que todo funcione perfectamente."
  }
];

// Estadísticas de confianza
const stats = [
      { value: "50+", label: "Proyectos Completados" },
      { value: "98%", label: "Satisfacción del Cliente" },
      { value: "24h", label: "Tiempo de Respuesta" },
      { value: "5★", label: "Calificación Promedio" }
];

// Testimonios
const testimonials = [
  {
    name: "María González",
    company: "TechStart",
    text: "Fenux transformó completamente nuestra presencia digital. El rediseño aumentó nuestras conversiones en un 300%.",
    avatar: "👩‍💼"
  },
  {
    name: "Carlos Rodríguez",
    company: "E-commerce Plus",
    text: "La landing page que crearon para nosotros generó más leads en una semana que en todo el año anterior.",
    avatar: "👨‍💻"
  },
  {
    name: "Ana Martínez",
    company: "Marketing Pro",
    text: "Profesionales excepcionales. Entregaron exactamente lo que prometieron, en tiempo y forma.",
    avatar: "👩‍🎨"
  }
];
---

<Base 
  title={pageConfig.title}
  description={pageConfig.description}
  canonical={pageConfig.canonical}
  ogImage={pageConfig.ogImage}
>
  <!-- Hero Section inspirado en React component -->
  <section class="relative overflow-hidden min-h-screen flex items-center bg-gray-950">
    <!-- Background radial gradient -->
    <div class="absolute top-0 z-[0] h-screen w-screen bg-purple-950/20 bg-[radial-gradient(ellipse_20%_80%_at_50%_-20%,rgba(120,119,198,0.25),rgba(255,255,255,0))]"></div>
    
    <!-- Retro Grid -->
    <div class="pointer-events-none absolute size-full overflow-hidden [perspective:200px] opacity-60">
      <div class="absolute inset-0 [transform:rotateX(65deg)]">
        <div class="animate-grid [background-image:linear-gradient(to_right,#2a2a2a_1px,transparent_0),linear-gradient(to_bottom,#2a2a2a_1px,transparent_0)] [background-repeat:repeat] [background-size:50px_50px] [height:300vh] [inset:0%_0px] [margin-left:-200%] [transform-origin:100%_0_0] [width:600vw]"></div>
      </div>
      <div class="absolute inset-0 bg-gradient-to-t from-gray-950 to-transparent to-90%"></div>
    </div>
    
    <div class="relative max-w-full mx-auto z-1">
      <div class="max-w-screen-xl z-10 mx-auto px-4 py-28 gap-12 md:px-8">
        <div class="space-y-5 max-w-3xl leading-0 lg:leading-5 mx-auto text-center">
          <!-- Badge con chevron -->
          <div class="text-sm text-gray-400 group mx-auto px-5 py-2 bg-gradient-to-tr from-zinc-300/5 via-gray-400/5 to-transparent border-[2px] border-white/5 rounded-3xl w-fit">
            Bienvenido a Fenux
            <svg class="inline w-4 h-4 ml-2 group-hover:translate-x-1 duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </div>
          
          <!-- Título principal con gradiente -->
          <h1 class="text-4xl tracking-tighter mx-auto md:text-6xl bg-clip-text text-transparent bg-[linear-gradient(180deg,_#FFF_0%,_rgba(255,_255,_255,_0.00)_202.08%)]">
            Transforma tus ideas en 
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-300 to-orange-200">
              experiencias digitales hermosas
            </span>
          </h1>
          
          <!-- Descripción -->
          <p class="max-w-2xl mx-auto text-gray-300 text-lg">
            Transforma tus ideas en realidad con nuestra suite completa de herramientas de desarrollo y recursos para crear apps web y landing pages de alto impacto.
          </p>
          
                  <!-- Dashboard Preview con mockups de la carpeta mockups -->
        <div class="mt-32 mx-10 relative z-10">
          <div class="relative">
            <!-- Laptop mockup -->
            <img 
              src="/images/mockups/laptop.png" 
              alt="Mockup de laptop con app web Fenux" 
              class="mx-auto drop-shadow-2xl w-full max-w-4xl shadow-2xl rounded-lg"
              loading="eager"
            />
            <!-- Mobile mockup -->
            <img 
              src="/images/mockups/mobile.png" 
              alt="Mockup de móvil con app Fenux" 
              class="absolute -bottom-8 -right-8 w-40 md:w-48 drop-shadow-xl shadow-2xl rounded-lg"
              loading="eager"
            />
          </div>
        </div>

          <!-- CTA con borde animado -->
          <div class="items-center justify-center gap-x-3 space-y-3 sm:flex sm:space-y-0">
            <span class="relative inline-block overflow-hidden rounded-full p-[1.5px]">
              <span class="absolute inset-[-1000%] animate-[spin_2s_linear_infinite] bg-[conic-gradient(from_90deg_at_50%_50%,#E2CBFF_0%,#393BB2_50%,#E2CBFF_100%)]"></span>
              <div class="inline-flex h-full w-full cursor-pointer items-center justify-center rounded-full bg-gray-950 text-xs font-medium backdrop-blur-3xl">
                  <a
                    href="#servicios"
                    class="inline-flex rounded-full text-center group items-center w-full justify-center bg-gradient-to-tr from-zinc-300/5 via-purple-400/20 text-white border-input border-[1px] hover:bg-gradient-to-tr hover:from-zinc-300/10 hover:via-purple-400/30 transition-all sm:w-auto py-4 px-10"
                    data-analytics="cta-servicios"
                  >
                    Conocer más
                  </a>
              </div>
            </span>
          </div>
        </div>
        

      </div>
    </div>
  </section>

  <!-- Nuestros Servicios -->
  <section id="servicios" class="py-16 md:py-24 bg-gray-900/50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Qué hacemos
        </h2>
        <p class="text-xl text-white/90 max-w-2xl mx-auto">
          Sin compromiso. Te mostramos funcionando antes de pagar.
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {services.map((service) => (
          <a 
            href={service.href}
            class="service-card group block"
            data-analytics={`cta-service-${service.title.toLowerCase().replace(' ', '-')}`}
          >
            <div class="mb-6">
              <div class="w-16 h-16 bg-gradient-to-r from-fuchsia-600 to-orange-500 rounded-xl flex items-center justify-center text-3xl group-hover:scale-110 transition-transform duration-300">
                {service.icon}
              </div>
            </div>
            <h3 class="text-xl font-bold text-white mb-3">{service.title}</h3>
            <p class="text-white/90 mb-4">{service.description}</p>
            <span class="text-fuchsia-400 font-medium group-hover:text-fuchsia-300 transition-colors">
              Explorar →
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Qué incluye -->
  <section id="oferta" class="py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Qué incluye
        </h2>
        <p class="text-xl text-white/90 max-w-2xl mx-auto">
          Todo lo necesario para que tu app web funcione desde el primer día
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {includes.map((item) => (
          <div class="feature-card group">
            <div class="w-16 h-16 bg-gradient-to-r from-fuchsia-600 to-orange-500 rounded-xl flex items-center justify-center mx-auto mb-6 text-3xl group-hover:scale-110 transition-transform duration-300">
              {item.icon}
            </div>
            <h3 class="text-xl font-bold text-white mb-3 text-center">{item.title}</h3>
            <p class="text-white/90 text-center">{item.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Estadísticas de confianza -->
  <section id="precios" class="py-16 md:py-24 bg-gray-900/50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Números que hablan
        </h2>
        <p class="text-xl text-white/90 max-w-2xl mx-auto">
          Resultados reales de nuestros proyectos
        </p>
      </div>
      
      <div class="grid md:grid-cols-4 gap-8">
        {stats.map((stat) => (
          <div class="text-center group">
            <div class="text-4xl md:text-5xl font-bold text-white mb-2 group-hover:scale-110 transition-transform duration-300">
              {stat.value}
            </div>
            <div class="text-white/90 text-lg">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Testimonios -->
  <section id="preguntas" class="py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Lo que dicen nuestros clientes
        </h2>
        <p class="text-xl text-white/90 max-w-2xl mx-auto">
          Casos reales de éxito con nuestras soluciones
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {testimonials.map((testimonial) => (
          <div class="testimonial-card group">
            <div class="flex items-center mb-4">
              <div class="w-12 h-12 bg-gradient-to-r from-fuchsia-600 to-orange-500 rounded-full flex items-center justify-center text-2xl mr-4">
                {testimonial.avatar}
              </div>
              <div>
                <div class="font-bold text-white">{testimonial.name}</div>
                <div class="text-white/80 text-sm">{testimonial.company}</div>
              </div>
            </div>
            <p class="text-white/95 italic">"{testimonial.text}"</p>
            <!-- Estrellas -->
            <div class="flex mt-4">
              {[...Array(5)].map((_, i) => (
                <svg class="w-4 h-4 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                </svg>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section id="contacto" class="py-16 md:py-24 bg-gradient-to-r from-fuchsia-900/50 to-orange-900/50">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
        ¿Listo para impulsar tu negocio?
      </h2>
      <p class="text-xl text-white/90 mb-8 max-w-2xl mx-auto">
        Reserva tu demo gratuita y descubre cómo podemos transformar tu presencia digital.
      </p>
      
      <div class="max-w-2xl mx-auto">
        <div class="bg-white/10 backdrop-blur-sm rounded-xl p-8 border border-white/20">
          <h3 class="text-2xl font-bold text-white mb-6">Reserva tu demo gratuita</h3>
          <LeadForm 
            source="home-final"
            showPhone={true}
            showMessage={false}
            buttonText="Reservar demo gratuita"
            className="space-y-4"
            inline={false}
          />
          <p class="text-sm text-white/90 mt-4">
            Sin compromiso. Te mostramos funcionando antes de pagar.
          </p>
        </div>
      </div>
    </div>
  </section>
</Base>

    <style>
  .service-card {
    @apply bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20 hover:bg-white/15 transition-all duration-300 hover:transform hover:scale-105 hover:shadow-xl hover:shadow-fuchsia-500/10;
  }
  
  .feature-card {
    @apply bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20 hover:bg-white/15 transition-all duration-300 hover:transform hover:scale-105 hover:shadow-xl hover:shadow-fuchsia-500/10;
  }
  
  .testimonial-card {
    @apply bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20 hover:bg-white/15 transition-all duration-300 hover:transform hover:scale-105 hover:shadow-xl hover:shadow-fuchsia-500/10;
  }
  
  /* Animación del grid retro */
  @keyframes grid {
    0% {
      transform: translateY(-50%);
    }
    100% {
      transform: translateY(0);
    }
  }
  
  .animate-grid {
    animation: grid 15s linear infinite;
  }
  
  /* Animaciones fade-up */
  .fade-up {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .fade-up.visible {
    opacity: 1;
    transform: translateY(0);
  }
  
  /* Animaciones suaves */
  @media (prefers-reduced-motion: no-preference) {
    .service-card, .feature-card, .testimonial-card {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    /* Animación de entrada para elementos */
    .animate-on-scroll {
      opacity: 0;
      transform: translateY(50px);
      transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .animate-on-scroll.visible {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  /* Reducir animaciones si el usuario lo prefiere */
  @media (prefers-reduced-motion: reduce) {
    .service-card, .feature-card, .testimonial-card {
      transition: none;
    }
    
    .animate-on-scroll {
      opacity: 1;
      transform: none;
    }
    
    .animate-grid {
      animation: none;
    }
  }
  
  /* Efectos adicionales para el hero */
  .hero-badge {
    backdrop-filter: blur(10px);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.05));
  }
  
  .hero-cta {
    backdrop-filter: blur(20px);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.05));
  }
    </style>

<!-- Schema JSON-LD -->
    <script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "Fenux",
  "description": "Soluciones Web y Apps que Impulsan tu Negocio. Diseño premium, automatización y ventas en un solo lugar.",
  "url": "https://fenux.com",
  "logo": "https://fenux.com/logos/logocompleto.png",
        "contactPoint": {
          "@type": "ContactPoint",
    "telephone": "+580000000000",
          "contactType": "customer service",
          "availableLanguage": ["Spanish", "English"]
        },
        "sameAs": [
          "https://github.com/fenux",
          "https://linkedin.com/company/fenux"
  ],
  "offers": [
    {
      "@type": "Offer",
      "name": "Rediseño Web",
      "description": "Modernizamos y mejoramos conversión de tu sitio actual"
    },
    {
      "@type": "Offer", 
      "name": "Landing Pages",
      "description": "Alta conversión en 5 días, sin pago adelantado"
    },
    {
      "@type": "Offer",
      "name": "Apps Web",
      "description": "Módulos completos: inventarios, pedidos, reportes"
    }
  ]
}
    </script>

<!-- Analytics tracking y animaciones -->
<script>
  // Track CTA clicks
  document.addEventListener('DOMContentLoaded', function() {
    const ctaButtons = document.querySelectorAll('[data-analytics]');
    
    ctaButtons.forEach(button => {
      button.addEventListener('click', function() {
        const action = this.getAttribute('data-analytics');
        
        // Google Analytics
        if (typeof gtag !== 'undefined') {
          gtag('event', 'click', {
            event_category: 'CTA',
            event_label: action
          });
        }
        
        // Meta Pixel
        if (typeof window !== 'undefined' && window.fbq) {
          window.fbq('track', 'Lead', { cta: action });
        }
        
        // Console log for debugging
        console.log('CTA clicked:', action);
      });
    });
    
    // Animaciones fade-up al scroll
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, observerOptions);
    
    // Observar elementos con animación
    const animatedElements = document.querySelectorAll('.service-card, .feature-card, .testimonial-card, h2, h3, p');
    animatedElements.forEach(el => {
      el.classList.add('animate-on-scroll');
      observer.observe(el);
    });
  });
    </script>
