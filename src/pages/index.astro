---
// Home Premium - Fenux
import Base from '../layouts/Base.astro';
import LeadForm from '../components/LeadForm.astro';

// Configuración de la página
const pageConfig = {
  title: "Fenux — Apps Web & Landing Pages que convierten",
  description: "Construimos apps web y landing pages con automatizaciones listas. Demo real sin pago adelantado.",
  canonical: "https://fenux.com",
  ogImage: "/og/home.png"
};

// Servicios clave
const services = [
  {
    title: "Rediseño Web",
    description: "Moderniza y mejora conversión de tu sitio actual",
    icon: "/mockups/feature-reportes.svg",
    href: "/es/servicios/redisenio"
  },
  {
    title: "Landing Pages",
    description: "Alta conversión en 5 días, sin pago adelantado",
    icon: "/mockups/feature-pedidos.svg",
    href: "/es/servicios/landing-portfolios"
  },
  {
    title: "Apps Web",
    description: "Módulos completos: inventario, pedidos, reportes, chatbot",
    icon: "/mockups/feature-inventario.svg",
    href: "/es/servicios/apps-web"
  }
];

// Beneficios diferenciales
const benefits = [
  {
    icon: "🎯",
    title: "Demo real sin pago",
    description: "Te mostramos funcionando antes de pagar"
  },
  {
    icon: "⚡",
    title: "Entrega rápida",
    description: "5 días para landing, 2-4 semanas para apps"
  },
  {
    icon: "🤖",
    title: "Automatizaciones listas",
    description: "WhatsApp, email, Sheets integrados"
  },
  {
    icon: "🛠️",
    title: "Soporte de arranque",
    description: "7-14 días de acompañamiento"
  }
];

// Proceso en 4 pasos
const process = [
  {
    step: "1",
    title: "Diagnóstico 10 min",
    description: "Entendemos tu negocio y definimos el alcance"
  },
  {
    step: "2", 
    title: "Demo con tus datos",
    description: "Te mostramos la solución funcionando con información real"
  },
  {
    step: "3",
    title: "Versión operativa",
    description: "Desarrollamos tu proyecto completo y funcional"
  },
  {
    step: "4",
    title: "Lanzamiento",
    description: "Ajustamos detalles y lanzamos al mundo"
  }
];

// Módulos disponibles
const modules = [
  {
    id: "inventario",
    title: "Inventario",
    description: "Control total de stock y alertas automáticas",
    icon: "/mockups/feature-inventario.svg"
  },
  {
    id: "pedidos",
    title: "Pedidos", 
    description: "Gestión completa del flujo de pedidos",
    icon: "/mockups/feature-pedidos.svg"
  },
  {
    id: "reportes",
    title: "Reportes",
    description: "Analytics avanzados y métricas de negocio",
    icon: "/mockups/feature-reportes.svg"
  },
  {
    id: "usuarios",
    title: "Usuarios",
    description: "Sistema de roles y permisos personalizado",
    icon: "/mockups/feature-usuarios.svg"
  },
  {
    id: "pagos",
    title: "Pagos",
    description: "Integración con múltiples pasarelas",
    icon: "/mockups/feature-pagos.svg"
  },
  {
    id: "chatbot",
    title: "Chatbot",
    description: "Asistente inteligente para atención al cliente",
    icon: "/mockups/feature-chatbot.svg"
  }
];

// Planes
const plans = [
  {
    name: "Start",
    price: "1,000",
    currency: "USD",
    features: [
      "1 módulo principal",
      "Sistema de autenticación", 
      "3 automatizaciones",
      "Soporte 7 días"
    ],
    popular: false
  },
  {
    name: "Pro",
    price: "1,500", 
    currency: "USD",
    features: [
      "3 módulos principales",
      "Sistema de autenticación",
      "5 automatizaciones", 
      "Integraciones avanzadas",
      "Soporte 14 días"
    ],
    popular: true
  }
];

// FAQ
const faqs = [
  {
    question: "¿Cuánto tarda?",
    answer: "Landing pages: 5 días. Apps web: 2-4 semanas. Siempre con demo previo."
  },
  {
    question: "¿Quién es dueño del código?",
    answer: "Tú. El código es 100% tuyo, incluyendo documentación completa."
  },
  {
    question: "¿Qué incluye la demo?",
    answer: "Te mostramos tu proyecto funcionando con datos reales de tu negocio."
  },
  {
    question: "¿Se integra con mis herramientas?",
    answer: "Sí, integramos con WhatsApp, Google Sheets, emails, pagos y más APIs."
  }
];

// Métricas
const stats = [
  { value: "50+", label: "Proyectos Completados" },
  { value: "98%", label: "Satisfacción del Cliente" },
  { value: "24h", label: "Tiempo de Respuesta" },
  { value: "5★", label: "Calificación Promedio" }
];
---

<Base 
  title={pageConfig.title}
  description={pageConfig.description}
  canonical={pageConfig.canonical}
  ogImage={pageConfig.ogImage}
>
  <!-- Hero Section -->
  <section class="relative overflow-hidden py-16 md:py-24">
    <!-- Background gradient -->
    <div class="absolute inset-0 -z-10 bg-gradient-to-b from-fuchsia-700/30 via-purple-900/20 to-transparent"></div>
    
    <!-- Background effects -->
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-fuchsia-500/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-orange-500/10 rounded-full blur-3xl"></div>
    
    <div class="container mx-auto px-4">
      <div class="grid gap-10 md:grid-cols-2 items-center">
        <!-- Content -->
        <div class="space-y-6">
          <h1 class="text-4xl md:text-6xl font-bold leading-tight text-white">
            Apps web y páginas que <span class="text-transparent bg-clip-text bg-gradient-to-r from-fuchsia-500 to-orange-400">generan clientes reales</span>.
          </h1>
          
          <p class="text-xl text-white/80 max-w-xl leading-relaxed">
            Diseño, desarrollo y automatización en una sola entrega. Demo real, sin pago adelantado.
          </p>
          
          <!-- CTAs -->
          <div class="flex flex-wrap gap-4">
            <a 
              href="https://astro-sitio.vercel.app/" 
              target="_blank"
              rel="noopener noreferrer"
              class="px-8 py-4 rounded-xl bg-gradient-to-r from-fuchsia-600 to-orange-500 text-white font-semibold shadow-lg hover:shadow-fuchsia-500/25 transition-all duration-300 transform hover:scale-105"
              data-analytics="cta-demo-hero"
            >
              Ver demo en vivo
            </a>
            
            <a 
              href={import.meta.env.PUBLIC_WHATSAPP_LINK || "https://wa.me/580000000000?text=Hola%20quiero%20la%20demo"}
              target="_blank"
              rel="noopener noreferrer"
              class="px-8 py-4 rounded-xl bg-white/10 hover:bg-white/15 text-white font-semibold transition-all duration-300 border border-white/20"
              data-analytics="cta-whatsapp-hero"
              aria-label="Hablar por WhatsApp"
            >
              Hablar por WhatsApp
            </a>
          </div>
          
          <!-- Mini form -->
          <div class="mt-8">
            <LeadForm 
              source="home-hero"
              showPhone={true}
              showMessage={false}
              buttonText="Reservar demo gratuita"
              className="bg-white/10 backdrop-blur rounded-xl p-6 ring-1 ring-white/10"
              inline={true}
            />
            <p class="text-xs text-white/60 mt-3 text-center">
              Sin compromiso. Te mostramos funcionando antes de pagar.
            </p>
          </div>
        </div>
        
        <!-- Mockups -->
        <div class="relative">
          <img 
            src="/mockups/hero-laptop.svg" 
            alt="Panel de app web Fenux" 
            class="mx-auto drop-shadow-2xl w-full max-w-lg"
            loading="eager"
          />
          <img 
            src="/mockups/hero-mobile.svg" 
            alt="App móvil Fenux" 
            class="absolute -bottom-6 -right-2 w-36 md:w-44 drop-shadow-xl"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Servicios clave -->
  <section class="py-16 md:py-24 bg-gray-900/50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Servicios clave
        </h2>
        <p class="text-xl text-white/70 max-w-2xl mx-auto">
          Soluciones específicas para cada necesidad de tu negocio
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {services.map((service) => (
          <a 
            href={service.href}
            class="glass-card group block"
            data-analytics={`cta-service-${service.title.toLowerCase().replace(' ', '-')}`}
          >
            <div class="mb-6">
              <img 
                src={service.icon} 
                alt={`Mockup de ${service.title}`}
                class="w-full h-20 object-contain group-hover:scale-105 transition-transform duration-300"
              />
            </div>
            <h3 class="text-xl font-semibold text-white mb-3">{service.title}</h3>
            <p class="text-white/70 mb-4">{service.description}</p>
            <span class="text-fuchsia-400 font-medium group-hover:text-fuchsia-300 transition-colors">
              Explorar →
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Beneficios diferenciales -->
  <section class="py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Por qué somos distintos
        </h2>
        <p class="text-xl text-white/70 max-w-2xl mx-auto">
          Ventajas que nos hacen únicos en el mercado
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {benefits.map((benefit) => (
          <div class="text-center group">
            <div class="w-16 h-16 bg-gradient-to-r from-fuchsia-600 to-orange-500 rounded-xl flex items-center justify-center mx-auto mb-6 text-3xl group-hover:scale-110 transition-transform duration-300">
              {benefit.icon}
            </div>
            <h3 class="text-lg font-semibold text-white mb-3">{benefit.title}</h3>
            <p class="text-white/70 text-sm">{benefit.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Proceso en 4 pasos -->
  <section class="py-16 md:py-24 bg-gray-900/50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Nuestro proceso
        </h2>
        <p class="text-xl text-white/70 max-w-2xl mx-auto">
          Proceso simple y transparente para obtener tu solución
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {process.map((step, index) => (
          <div class="text-center group">
            <div class="w-20 h-20 bg-gradient-to-r from-fuchsia-600 to-orange-500 rounded-full flex items-center justify-center mx-auto mb-6 text-2xl font-bold text-white group-hover:scale-110 transition-transform duration-300">
              {step.step}
            </div>
            <h3 class="text-xl font-semibold text-white mb-3">{step.title}</h3>
            <p class="text-white/70">{step.description}</p>
          </div>
        ))}
      </div>
      
      <div class="text-center mt-12">
        <a 
          href="https://astro-sitio.vercel.app/" 
          target="_blank"
          rel="noopener noreferrer"
          class="px-8 py-4 rounded-xl bg-gradient-to-r from-fuchsia-600 to-orange-500 text-white font-semibold hover:shadow-lg hover:shadow-fuchsia-500/25 transition-all duration-300 transform hover:scale-105"
          data-analytics="cta-demo-process"
        >
          Ver demo ahora
        </a>
      </div>
    </div>
  </section>

  <!-- Módulos disponibles -->
  <section class="py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Módulos disponibles
        </h2>
        <p class="text-xl text-white/70 max-w-2xl mx-auto">
          Cada módulo resuelve necesidades específicas de tu negocio
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {modules.map((module) => (
          <div class="glass-card group">
            <div class="mb-6">
              <img 
                src={module.icon} 
                alt={`Mockup de ${module.title}`}
                class="w-full h-20 object-contain group-hover:scale-105 transition-transform duration-300"
              />
            </div>
            <h3 class="text-xl font-semibold text-white mb-3">{module.title}</h3>
            <p class="text-white/70">{module.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Prueba social -->
  <section class="py-16 md:py-24 bg-gray-900/50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Números que hablan
        </h2>
        <p class="text-xl text-white/70 max-w-2xl mx-auto">
          Resultados reales de nuestros proyectos
        </p>
      </div>
      
      <div class="grid md:grid-cols-4 gap-8 mb-16">
        {stats.map((stat) => (
          <div class="text-center">
            <div class="text-4xl md:text-5xl font-bold text-white mb-2">
              {stat.value}
            </div>
            <div class="text-white/70">{stat.label}</div>
          </div>
        ))}
      </div>
      
      <!-- Logos de herramientas -->
      <div class="text-center">
        <p class="text-white/60 mb-8">Tecnologías que utilizamos</p>
        <div class="flex flex-wrap justify-center items-center gap-8 opacity-60">
          <div class="text-white/40 font-semibold">Astro</div>
          <div class="text-white/40 font-semibold">Vercel</div>
          <div class="text-white/40 font-semibold">WhatsApp</div>
          <div class="text-white/40 font-semibold">Google Sheets</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Planes -->
  <section class="py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Planes claros
        </h2>
        <p class="text-xl text-white/70 max-w-2xl mx-auto">
          Precios transparentes sin sorpresas
        </p>
      </div>
      
      <!-- Badge -->
      <div class="text-center mb-12">
        <div class="inline-flex items-center px-6 py-3 rounded-full bg-green-500/10 border border-green-500/30 text-green-200 font-medium">
          <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          Sin pago adelantado — pagas al aprobar la demo
        </div>
      </div>
      
      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        {plans.map((plan) => (
          <div class={`glass-card ${plan.popular ? 'border-2 border-fuchsia-500/50 relative' : ''}`}>
            {plan.popular && (
              <div class="absolute -top-4 left-1/2 transform -translate-x-1/2">
                <span class="bg-gradient-to-r from-fuchsia-600 to-orange-500 text-white px-4 py-2 rounded-full text-sm font-semibold">
                  Más popular
                </span>
              </div>
            )}
            
            <div class="text-center mb-8">
              <h3 class="text-2xl font-bold text-white mb-2">{plan.name}</h3>
              <div class="text-4xl font-bold text-white mb-2">${plan.price}</div>
              <div class="text-white/70">{plan.currency}</div>
            </div>
            
            <ul class="space-y-4 mb-8">
              {plan.features.map((feature) => (
                <li class="flex items-center text-white/80">
                  <svg class="w-5 h-5 text-green-400 mr-3" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                  </svg>
                  {feature}
                </li>
              ))}
            </ul>
            
            <button 
              class="w-full py-3 rounded-xl bg-gradient-to-r from-fuchsia-600 to-orange-500 text-white font-semibold hover:shadow-lg hover:shadow-fuchsia-500/25 transition-all duration-300"
              onclick="document.querySelector('.hero-form').scrollIntoView({behavior: 'smooth'})"
              data-analytics={`cta-reservar-${plan.name.toLowerCase()}`}
            >
              Reservar demo
            </button>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="py-16 md:py-24 bg-gray-900/50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
          Preguntas frecuentes
        </h2>
        <p class="text-xl text-white/70 max-w-2xl mx-auto">
          Respuestas a las dudas más comunes
        </p>
      </div>
      
      <div class="max-w-3xl mx-auto space-y-6">
        {faqs.map((faq) => (
          <div class="glass-card">
            <h3 class="text-lg font-semibold text-white mb-3">{faq.question}</h3>
            <p class="text-white/80">{faq.answer}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section class="py-16 md:py-24 bg-gradient-to-r from-fuchsia-900/50 to-orange-900/50">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-3xl md:text-5xl font-bold text-white mb-6">
        Tu app operativa en días, no meses
      </h2>
      <p class="text-xl text-white/80 mb-8 max-w-2xl mx-auto">
        No más esperas de meses. Construimos tu solución y la tienes funcionando en semanas.
      </p>
      
      <div class="flex flex-wrap justify-center gap-4">
        <a 
          href="https://astro-sitio.vercel.app/" 
          target="_blank"
          rel="noopener noreferrer"
          class="px-8 py-4 rounded-xl bg-gradient-to-r from-fuchsia-600 to-orange-500 text-white font-semibold hover:shadow-lg hover:shadow-fuchsia-500/25 transition-all duration-300 transform hover:scale-105"
          data-analytics="cta-demo-final"
        >
          Probar demo en vivo
        </a>
        
        <a 
          href={import.meta.env.PUBLIC_WHATSAPP_LINK || "https://wa.me/580000000000?text=Hola%20quiero%20la%20demo"}
          target="_blank"
          rel="noopener noreferrer"
          class="px-8 py-4 rounded-xl bg-white/10 backdrop-blur-sm text-white font-semibold hover:bg-white/20 transition-all duration-300 border border-white/20"
          data-analytics="cta-whatsapp-final"
        >
          Hablar por WhatsApp
        </a>
      </div>
    </div>
  </section>

  <!-- Formulario de reserva (reutilizar el del hero) -->
  <section id="reserva" class="py-16 md:py-24 bg-gray-900/50">
    <div class="container mx-auto px-4">
      <div class="max-w-2xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
          Reserva tu demo gratuita
        </h2>
        <p class="text-xl text-white/70 mb-8">
          Sin compromiso. Te mostramos tu solución funcionando antes de pagar.
        </p>
        
        <div class="hero-form">
          <LeadForm 
            source="home-reserva"
            showPhone={true}
            showMessage={true}
            buttonText="Reservar demo gratuita"
            className="bg-white/10 backdrop-blur-sm rounded-xl p-8 border border-white/20"
          />
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  .glass-card {
    @apply bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20 hover:bg-white/15 transition-all duration-300 hover:transform hover:scale-105 hover:shadow-xl hover:shadow-fuchsia-500/10;
  }
  
  /* Animaciones suaves */
  @media (prefers-reduced-motion: no-preference) {
    .glass-card {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
  }
  
  /* Reducir animaciones si el usuario lo prefiere */
  @media (prefers-reduced-motion: reduce) {
    .glass-card {
      transition: none;
    }
  }
</style>

<!-- Schema JSON-LD -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "Fenux",
  "description": "Construimos apps web y landing pages con automatizaciones listas. Demo real sin pago adelantado.",
  "url": "https://fenux.com",
  "logo": "https://fenux.com/logos/logocompleto.png",
  "contactPoint": {
    "@type": "ContactPoint",
    "telephone": "+580000000000",
    "contactType": "customer service",
    "availableLanguage": ["Spanish", "English"]
  },
  "sameAs": [
    "https://github.com/fenux",
    "https://linkedin.com/company/fenux"
  ],
  "offers": [
    {
      "@type": "Offer",
      "name": "Start",
      "price": "1000",
      "priceCurrency": "USD",
      "description": "1 módulo principal, sistema de autenticación, 3 automatizaciones, soporte 7 días"
    },
    {
      "@type": "Offer", 
      "name": "Pro",
      "price": "1500",
      "priceCurrency": "USD",
      "description": "3 módulos principales, sistema de autenticación, 5 automatizaciones, integraciones avanzadas, soporte 14 días"
    }
  ]
}
</script>

<!-- Analytics tracking -->
<script>
  // Track CTA clicks
  document.addEventListener('DOMContentLoaded', function() {
    const ctaButtons = document.querySelectorAll('[data-analytics]');
    
    ctaButtons.forEach(button => {
      button.addEventListener('click', function() {
        const action = this.getAttribute('data-analytics');
        
        // Google Analytics
        if (typeof gtag !== 'undefined') {
          gtag('event', 'click', {
            event_category: 'CTA',
            event_label: action
          });
        }
        
        // Meta Pixel
        if (typeof window !== 'undefined' && window.fbq) {
          window.fbq('track', 'Lead', { cta: action });
        }
        
        // Console log for debugging
        console.log('CTA clicked:', action);
      });
    });
  });
</script>
